# Rack + FlexDoc

This example uses `prauga-flexdoc` `0.3.0` directly as a Rack application.

```bash
bundle install
bundle exec rackup
```

Open `/docs`. The same `RackApp` is the transport used by the Rails helper.
