# Gin + FlexDoc

Gin can wrap the existing FlexDoc `net/http` handler directly with `gin.WrapH`, so no Gin-specific renderer adapter is required. This example uses `github.com/prauga/flexdoc/adapters/go v0.3.0`.
